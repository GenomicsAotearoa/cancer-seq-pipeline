qc = {
    exec "bash ./fastqc_array.sl $input "
}
trim = {
    exec "trimGalore_array.sl $input"
}

count = {
  exec "Rscript filePairs.R  - $input > $output"
}
merge = {
   exec "Rscript merge.R"
} 
	
report = {
    exec "echo World | cat $input - > $output"
}

run {trim}
